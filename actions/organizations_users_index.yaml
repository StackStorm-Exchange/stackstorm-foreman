---
description: "List all users for organization (resource: GET /api/organizations/:organization_id/users)"
enabled: true
runner_type: "python-script"
entry_point: 
name: organizations_users_index
parameters:
  operation:
    type: string
    default: organizations.users_index
    immutable: true
  connection:
    type: string
    description: "Name of <connection> from this pack's configuration that specifies how to connect to a server."
    required: false
  server:
    type: string
    description: "Optional override of the server in <connection> (required if <connection> is not specified)."
    required: false
  username:
    type: string
    description: "Optional override of the username in <connection> (example: username@domain.tld) (required if <connection> is not specified)."
    required: false
  password:
    type: string
    description: "Optional override of the password in <connection>. (required if <connection> is not specified)"
    required: false
    secret: true
  port:
    type: integer
    description: "Optional override of the port in <connection>."
    required: false
  transport:
    type: string
    description: "Optional override of the transport in <connection>."
    required: false
  auth_source_ldap_id:
    type: string
    required: false
    description: ID of LDAP authentication source-
  usergroup_id:
    type: string
    required: false
    description: ID of user group-
  role_id:
    type: string
    required: false
    description: ID of role-
  location_id:
    type: numeric
    required: false
    description: Scope by locations-
  organization_id:
    type: numeric
    required: true
    description: Scope by organizations-
  search:
    type: string
    required: false
    description: filter results-
  order:
    type: string
    required: false
    description: sort results-
  page:
    type: string
    required: false
    description: paginate results-
  per_page:
    type: string
    required: false
    description: number of entries per request-