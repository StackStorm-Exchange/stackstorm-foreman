---
description: "Show a content view puppet module (resource: GET /katello/api/content_views/:content_view_id/content_view_puppet_modules/:id)"
enabled: true
runner_type: "python-script"
entry_point: 
name: content_views_content_view_puppet_modules_ontent_showviews
parameters:
  operation:
    type: string
    default: content_views.content_view_puppet_modules_ontent_showviews
    immutable: true
  connection:
    type: string
    description: "Name of <connection> from this pack's configuration that specifies how to connect to a server."
    required: false
  server:
    type: string
    description: "Optional override of the server in <connection> (required if <connection> is not specified)."
    required: false
  username:
    type: string
    description: "Optional override of the username in <connection> (example: username@domain.tld) (required if <connection> is not specified)."
    required: false
  password:
    type: string
    description: "Optional override of the password in <connection>. (required if <connection> is not specified)"
    required: false
    secret: true
  port:
    type: integer
    description: "Optional override of the port in <connection>."
    required: false
  transport:
    type: string
    description: "Optional override of the transport in <connection>."
    required: false
  content_view_id:
    type: string
    required: true
    description: content view numeric identifier-
  id:
    type: string
    required: true
    description: puppet module ID-