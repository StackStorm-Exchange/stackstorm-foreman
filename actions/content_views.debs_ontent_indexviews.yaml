---
description: "List deb packages (resource: GET /katello/api/content_views/:content_view_id/filters/:filter_id/debs)"
enabled: true
runner_type: "python-script"
entry_point: lib/action.py
name: content_views.debs_ontent_indexviews
pack: foreman
parameters:
  operation:
    type: string
    default: content_views.debs_ontent_indexviews
    immutable: true
  connection:
    type: string
    description: "Name of <connection> from this pack's configuration that specifies how to connect to a server."
    required: false
  server:
    type: string
    description: "Optional override of the server in <connection> (required if <connection> is not specified)."
    required: false
  username:
    type: string
    description: "Optional override of the username in <connection> (example: username@domain.tld) (required if <connection> is not specified)."
    required: false
  password:
    type: string
    description: "Optional override of the password in <connection>. (required if <connection> is not specified)"
    required: false
    secret: true
  organization_id:
    type: number
    required: false
    description: "Organization identifier"
  content_view_version_id:
    type: number
    required: false
    description: "Content View Version identifier"
  content_view_filter_id:
    type: number
    required: false
    description: "Content View Filter identifier"
  repository_id:
    type: number
    required: false
    description: "Repository identifier"
  environment_id:
    type: number
    required: false
    description: "Environment identifier"
  ids:
    type: array
    required: false
    description: "Deb package identifiers to filter content by"
  host_id:
    type: number
    required: false
    description: "Host id to list applicable deb packages for"
  packages_restrict_applicable:
    type: boolean
    required: false
    description: "Return deb packages that are applicable to one or more hosts (defaults totrue if host_id is specified)"
  packages_restrict_upgradable:
    type: boolean
    required: false
    description: "Return deb packages that are upgradable on one or more hosts"
  packages_restrict_latest:
    type: boolean
    required: false
    description: "Return only the latest version of each package"
  available_for:
    type: string
    required: false
    description: "Return deb packages that can be added to the specified object.  Only thevalue 'content_view_version' is supported."
  search:
    type: string
    required: false
    description: "Search string"
  page:
    type: number
    required: false
    description: "Page number, starting at 1"
  per_page:
    type: number
    required: false
    description: "Number of results per page to return"
  order:
    type: string
    required: false
    description: "Sort field and order, eg. 'id DESC'"
  full_result:
    type: boolean
    required: false
    description: "Whether or not to show all results"
  sort_by:
    type: string
    required: false
    description: "Field to sort the results on"
  sort_order:
    type: string
    required: false
    description: "How to order the sorted results (e.g. ASC for ascending)"