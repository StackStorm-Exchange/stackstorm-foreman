---
description: "show filter info (resource: get /katello/api/content_views/:content_view_id/filters/:id)"
enabled: true
runner_type: "python-script"
entry_point: lib/action.py
name: content_views_content_view_filters_ontent_showviews
parameters:
  operation:
    type: string
    default: content_views.content_view_filters_ontent_showviews
    immutable: true
  connection:
    type: string
    description: "Name of <connection> from this pack's configuration that specifies how to connect to a server."
    required: false
  server:
    type: string
    description: "Optional override of the server in <connection> (required if <connection> is not specified)."
    required: false
  username:
    type: string
    description: "Optional override of the username in <connection> (example: username@domain.tld) (required if <connection> is not specified)."
    required: false
  password:
    type: string
    description: "Optional override of the password in <connection>. (required if <connection> is not specified)"
    required: false
    secret: true
  content_view_id:
    type: string
    required: true
    description: "content view identifier"
  id:
    type: string
    required: true
    description: "filter identifier"