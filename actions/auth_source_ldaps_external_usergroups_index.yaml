---
description: "List all external user groups for LDAP authentication source (resource: GET /api/auth_source_ldaps/:auth_source_ldap_id/external_usergroups)"
enabled: true
runner_type: "python-script"
entry_point: 
name: auth_source_ldaps_external_usergroups_index
parameters:
  operation:
    type: string
    default: auth_source_ldaps.external_usergroups_index
    immutable: true
  connection:
    type: string
    description: "Name of <connection> from this pack's configuration that specifies how to connect to a server."
    required: false
  server:
    type: string
    description: "Optional override of the server in <connection> (required if <connection> is not specified)."
    required: false
  username:
    type: string
    description: "Optional override of the username in <connection> (example: username@domain.tld) (required if <connection> is not specified)."
    required: false
  password:
    type: string
    description: "Optional override of the password in <connection>. (required if <connection> is not specified)"
    required: false
    secret: true
  port:
    type: integer
    description: "Optional override of the port in <connection>."
    required: false
  transport:
    type: string
    description: "Optional override of the transport in <connection>."
    required: false
  usergroup_id:
    type: string
    required: true
    description: ID or name of user group-